<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.study.login.UserMapper">
    <select id="findAll" parameterType="map" resultType="com.study.login.UserDto">
        SELECT * FROM TOTAL_BOARD
    </select>
</mapper>